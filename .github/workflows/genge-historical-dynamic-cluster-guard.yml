name: GenGe Historical Dynamic Cluster Guard

on:
  pull_request:
    paths:
      - "src/strategies/genge_opportunity_discovery/historical_dynamic_cluster_guard.py"
      - "src/strategies/genge_opportunity_discovery/historical_correlation_cluster_guard.py"
      - "src/strategies/genge_opportunity_discovery/historical_concentration_guard.py"
      - "src/strategies/genge_opportunity_discovery/historical_portfolio_risk_budget.py"
      - "src/strategies/genge_opportunity_discovery/drawdown_risk_policy.py"
      - "src/strategies/genge_opportunity_discovery/hard_logic_historical_backtest.py"
      - "tests/test_genge_historical_dynamic_cluster_guard.py"
      - "config/genge_hard_logic_famous_cases.csv"
      - "config/board_risk_rules.yaml"
      - ".github/workflows/genge-historical-dynamic-cluster-guard.yml"
  workflow_dispatch:
    inputs:
      start_date:
        description: "Historical replay start date"
        required: false
        default: "2018-01-01"
        type: string
      end_date:
        description: "Historical replay end date"
        required: false
        default: "2026-08-18"
        type: string

permissions:
  contents: read

concurrency:
  group: genge-historical-dynamic-cluster-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

jobs:
  deterministic-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install CI dependencies
        run: |
          python -m pip install --upgrade pip --retries 5 --timeout 60
          python -m pip install --prefer-binary --retries 5 --timeout 60 -r .github/requirements-ci.txt
      - name: Validate dynamic cluster timing
        run: |
          python -m py_compile src/strategies/genge_opportunity_discovery/historical_dynamic_cluster_guard.py
          python -m pytest -q tests/test_genge_historical_dynamic_cluster_guard.py

  public-data-diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install CI dependencies
        run: |
          python -m pip install --upgrade pip --retries 5 --timeout 60
          python -m pip install --prefer-binary --retries 5 --timeout 60 -r .github/requirements-ci.txt
      - name: Cache historical public data
        uses: actions/cache@v4
        with:
          path: data/cache/hard_logic_history_backtest
          key: hard-logic-history-v3-${{ runner.os }}-${{ hashFiles('config/genge_hard_logic_famous_cases.csv') }}
      - name: Resolve replay dates
        shell: bash
        run: |
          echo "START_DATE=${{ inputs.start_date || '2018-01-01' }}" >> "$GITHUB_ENV"
          echo "END_DATE=${{ inputs.end_date || '2026-08-18' }}" >> "$GITHUB_ENV"
      - name: Run dynamic correlation cluster comparison
        run: |
          PYTHONUNBUFFERED=1 python -m src.strategies.genge_opportunity_discovery.historical_dynamic_cluster_guard \
            --cases-file config/genge_hard_logic_famous_cases.csv \
            --start-date "$START_DATE" \
            --end-date "$END_DATE" \
            --output-dir reports/historical_dynamic_cluster_guard \
            --cache-dir data/cache/hard_logic_history_backtest \
            --board-rules-file config/board_risk_rules.yaml \
            --evaluation-stride 5 \
            --cost-bps-per-side 15
      - name: Validate output contract
        run: |
          python - <<'PY'
          import csv
          import json
          from pathlib import Path
          root = Path('reports/historical_dynamic_cluster_guard')
          required = [
              root/'historical_dynamic_cluster_guard_summary.json',
              root/'historical_dynamic_cluster_guard.md',
              root/'dynamic_cluster_comparison.csv',
              root/'dynamic_cluster_allocations.csv',
              root/'dynamic_cluster_trims.csv',
              root/'data_failures.csv',
          ]
          for path in required:
              assert path.exists() and path.stat().st_size > 0, path
          summary = json.loads((root/'historical_dynamic_cluster_guard_summary.json').read_text(encoding='utf-8'))
          assert summary['stock_entry_exit_signal_set_frozen'] is True
          assert summary['dynamic_cluster_uses_only_history_through_close'] is True
          assert summary['dynamic_cluster_requires_weight_and_stress'] is True
          assert summary['weakest_members_trimmed_first'] is True
          assert summary['trim_decision_after_close_for_next_open'] is True
          assert summary['trim_shares_frozen_before_next_open'] is True
          assert summary['partial_trim_releases_open_risk_budget'] is True
          assert summary['famous_case_selection_bias_warning'] is True
          assert summary['survivorship_aware_all_a_required'] is True
          assert summary['production_deployment_allowed'] is False
          assert summary['frozen_event_count'] > 0
          rows = list(csv.DictReader((root/'dynamic_cluster_comparison.csv').open(encoding='utf-8')))
          assert len(rows) >= 3
          print(json.dumps(summary, ensure_ascii=False, indent=2))
          PY
      - name: Publish summary
        if: always()
        run: |
          if [ -f reports/historical_dynamic_cluster_guard/historical_dynamic_cluster_guard.md ]; then
            cat reports/historical_dynamic_cluster_guard/historical_dynamic_cluster_guard.md >> "$GITHUB_STEP_SUMMARY"
          fi
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: genge-historical-dynamic-cluster-guard
          path: reports/historical_dynamic_cluster_guard
          if-no-files-found: error
