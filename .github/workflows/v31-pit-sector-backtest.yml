name: V3.1 PIT sector backtest

# Locked-rule audit run. Do not tune strategy thresholds after seeing results.
# Data-source fallback changes transport only; strategy semantics stay frozen.
on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/v31_pit_sector_backtest.py'
      - 'scripts/v31_pit_sector_backtest_resilient.py'
      - '.github/workflows/v31-pit-sector-backtest.yml'
  pull_request:
    paths:
      - 'scripts/v31_pit_sector_backtest.py'
      - 'scripts/v31_pit_sector_backtest_resilient.py'
      - '.github/workflows/v31-pit-sector-backtest.yml'

permissions:
  contents: write

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install locked runtime dependencies
        run: |
          python -m pip install --upgrade pip
          pip install 'akshare>=1.12.0' 'pandas>=2.0.0' 'numpy>=1.24.0' tabulate 'yfinance>=0.2.0'
      - name: Run V3.1 PIT test with resilient public-data transport
        working-directory: scripts
        run: python v31_pit_sector_backtest_resilient.py
      - name: Persist auditable results in repository
        if: github.event_name == 'push'
        run: |
          rm -rf backtests/v31_pit_latest
          mkdir -p backtests/v31_pit_latest
          cp -a scripts/artifacts/v31_pit_backtest/. backtests/v31_pit_latest/
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add backtests/v31_pit_latest
          if ! git diff --cached --quiet; then
            git commit -m 'Store locked V3.1 PIT backtest results'
            git push
          fi
      - name: Upload full audit artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v31-pit-sector-backtest
          path: scripts/artifacts/v31_pit_backtest/
          if-no-files-found: warn
