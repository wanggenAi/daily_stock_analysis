name: GenGe Historical Fundamental Deterioration Guard

on:
  pull_request:
    paths:
      - "src/strategies/genge_opportunity_discovery/historical_fundamental_deterioration_guard.py"
      - "src/strategies/genge_opportunity_discovery/historical_fundamental_deterioration_audit.py"
      - "src/strategies/genge_opportunity_discovery/historical_winner_trend_guard.py"
      - "src/strategies/genge_opportunity_discovery/historical_concentration_guard.py"
      - "src/strategies/genge_opportunity_discovery/historical_portfolio_risk_budget.py"
      - "src/strategies/genge_opportunity_discovery/hard_logic_historical_backtest.py"
      - "tests/test_genge_historical_fundamental_deterioration_guard.py"
      - "config/genge_hard_logic_famous_cases.csv"
      - "config/board_risk_rules.yaml"
      - ".github/workflows/genge-historical-fundamental-deterioration-guard.yml"
  workflow_dispatch:
    inputs:
      start_date:
        description: "Historical replay start date"
        required: false
        default: "2018-01-01"
        type: string
      end_date:
        description: "Historical replay end date"
        required: false
        default: "2026-08-18"
        type: string

permissions:
  contents: read

concurrency:
  group: genge-historical-fundamental-guard-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

jobs:
  deterministic-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install CI dependencies
        run: |
          python -m pip install --upgrade pip --retries 5 --timeout 60
          python -m pip install --prefer-binary --retries 5 --timeout 60 -r .github/requirements-ci.txt
      - name: Validate confirmed fundamental exit timing
        run: |
          python -m py_compile src/strategies/genge_opportunity_discovery/historical_fundamental_deterioration_guard.py
          python -m pytest -q tests/test_genge_historical_fundamental_deterioration_guard.py

  public-data-diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install CI dependencies
        run: |
          python -m pip install --upgrade pip --retries 5 --timeout 60
          python -m pip install --prefer-binary --retries 5 --timeout 60 -r .github/requirements-ci.txt
      - name: Cache historical public data
        uses: actions/cache@v4
        with:
          path: data/cache/hard_logic_history_backtest
          key: hard-logic-history-v3-${{ runner.os }}-${{ hashFiles('config/genge_hard_logic_famous_cases.csv') }}
      - name: Resolve replay dates
        shell: bash
        run: |
          echo "START_DATE=${{ inputs.start_date || '2018-01-01' }}" >> "$GITHUB_ENV"
          echo "END_DATE=${{ inputs.end_date || '2026-08-18' }}" >> "$GITHUB_ENV"
      - name: Run confirmed fundamental deterioration exit comparison
        run: |
          PYTHONUNBUFFERED=1 python -m src.strategies.genge_opportunity_discovery.historical_fundamental_deterioration_guard \
            --cases-file config/genge_hard_logic_famous_cases.csv \
            --start-date "$START_DATE" \
            --end-date "$END_DATE" \
            --output-dir reports/historical_fundamental_deterioration_guard \
            --cache-dir data/cache/hard_logic_history_backtest \
            --board-rules-file config/board_risk_rules.yaml \
            --evaluation-stride 5 \
            --cost-bps-per-side 15
      - name: Validate output contract
        run: |
          python - <<'PY'
          import csv
          import json
          from pathlib import Path
          root = Path('reports/historical_fundamental_deterioration_guard')
          required = [
              root/'historical_fundamental_deterioration_guard_summary.json',
              root/'historical_fundamental_deterioration_guard.md',
              root/'fundamental_guard_comparison.csv',
              root/'fundamental_guard_early_exits.csv',
              root/'data_failures.csv',
          ]
          for path in required:
              assert path.exists() and path.stat().st_size > 0, path
          summary = json.loads((root/'historical_fundamental_deterioration_guard_summary.json').read_text(encoding='utf-8'))
          assert summary['entry_signal_set_frozen'] is True
          assert summary['original_exit_replaced_only_if_fundamental_exit_is_earlier'] is True
          assert summary['fundamental_decision_uses_effective_disclosure_date'] is True
          assert summary['fundamental_exit_executes_next_observed_open'] is True
          assert summary['optional_ma60_confirmation_uses_warning_date_only'] is True
          assert summary['partial_concentration_trim_releases_open_risk_budget'] is True
          assert summary['famous_case_selection_bias_warning'] is True
          assert summary['survivorship_aware_all_a_required'] is True
          assert summary['production_deployment_allowed'] is False
          assert summary['frozen_event_count'] > 0
          rows = list(csv.DictReader((root/'fundamental_guard_comparison.csv').open(encoding='utf-8')))
          assert len(rows) >= 4
          print(json.dumps(summary, ensure_ascii=False, indent=2))
          PY
      - name: Publish summary
        if: always()
        run: |
          if [ -f reports/historical_fundamental_deterioration_guard/historical_fundamental_deterioration_guard.md ]; then
            cat reports/historical_fundamental_deterioration_guard/historical_fundamental_deterioration_guard.md >> "$GITHUB_STEP_SUMMARY"
          fi
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: genge-historical-fundamental-deterioration-guard
          path: reports/historical_fundamental_deterioration_guard
          if-no-files-found: error
